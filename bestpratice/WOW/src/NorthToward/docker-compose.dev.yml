services:
  northtoward-web-dev:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://host.docker.internal:8080}
    volumes:
      - .:/app
      - /app/node_modules
