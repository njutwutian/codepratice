services:
  northtoward-web-prod:
    build:
      context: .
      target: prod
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080}
    ports:
      - "8088:80"
    restart: unless-stopped
