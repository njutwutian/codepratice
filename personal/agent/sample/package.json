{
  "name": "agent-sample-tian",
  "type": "module",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "dev:dev": "cross-env NODE_ENV=dev tsx src/index.ts",
    "dev:test": "cross-env NODE_ENV=test tsx src/index.ts",
    "start": "npm run build && node dist/index.js",
    "start:dev": "cross-env NODE_ENV=dev npm run start",
    "start:test": "cross-env NODE_ENV=test npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^25.9.1",
    "cross-env": "^10.1.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "dotenv-flow": "^4.1.0",
    "envalid": "^8.2.0"
  }
}
